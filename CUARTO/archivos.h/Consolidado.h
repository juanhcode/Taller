#include "Llamadas.h"
class Consolidado
{
private:
       /* data */
public:
    int costoTresLineas(int minLlamadaLocal, int minLlamadaLarga, int minLlamadaCelular);
    Consolidado(/* args */);
    ~Consolidado();
};
